定位系统info
1.echarts,element-ui,font-awesome,vue-resource,vue-router,vuex
2.20170419导航图的添加，用户信息的显示，用户增删改查(ok)
3.sweet alert 和vue-resource的拦截器做等待状态(ok)
4.退出按钮放在右边，加上用户名字(ok)
5导航图的删除(ok)
6location的左侧按钮组，添加最小化功能(ok)
7实现记住密码：登录时，如果选中记住密码，则将用户账号和密码存在localstorage里，下次登录时在mounted里获取账号和密码并绑定在input（this.username和this.password）上,这样密码就不安全了，所以只做记住账号，不做记住密码,但是这次的前端做的是浏览器封装，所以可以做，
8每次post时，带上token，这个并不需要在每个post里写，只需写个拦截器
https://juejin.im/post/58da720b570c350058ecd40f
9.页面加载，表单自动获取焦点：autofocus=true

D:\Android\sdk\tools>emulator.exe -netdelay none -netspeed full -avd react_avd
1、使用轮播插件vue-slide（引用jq后，选择变多）
2、tdata必须使用vue的懒加载，不然首次加载时间太长
3.添加和删除产品的时候，应该是先操作本地的数组（pop，splice等），再操作数据库
4重复邮箱注册，提示已经注册，返回登录页(个人的页面完毕)
5文档虽然是静态的，但是还是应该把东西都写在js变量里，
然后用v-for，之前写的几个页面，先不要改。下个页面用v-for
6短信验证荣联运(ok)
6.1获取验证后的倒计时(ok)
7订单页面（写在充值下面）(ok)
8动态书签锚点(第一页ok)
9后端两个邮箱样式（ok）
10建站参考：onenet，七牛，掘金，简书
11充值0.25元失败（小数舍去问题）（ok）
12seo优化
13onerror="this.src='/static/image/037-graduation.png'"
14detached HEAd(git问题)
15tdata前端页面的resource里有源文件，怎么避免
16汉坤官网404 500页面问题
17ping++的前端怎么写(ok)
18第三方账号登录
19射频识别，RFID（Radio Frequency Identification）技术，又称无线射频识别，是一种通信技术
20tdata更换头像后，旧的图片未删除
21(修复)公开协议添加为6个时，不能修改
	追踪：先添加公有，私有弹窗有bug，先添加私有，公有有bug
22，前端post时，应该携带token，这样才能验证token是否过期(拦截器)
